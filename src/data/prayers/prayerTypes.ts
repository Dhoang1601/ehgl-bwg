// This file's content has been moved to types.ts
// You can delete this file if types.ts is the preferred central location for all type definitions.
// Keeping it ensures no import errors if other files were already pointing here,
// but it's good practice to consolidate type definitions.

// export type PrayerCategoryKey = 'xungtoi' | 'themsuc' | 'songdao';

// export interface Prayer {
//   id: string;
//   categoryKey: PrayerCategoryKey;
//   title: string;
//   content: string[]; // Array of paragraphs
// }
